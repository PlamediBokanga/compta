import { useMemo, useState, type ReactNode } from 'react';
import {
  AlertTriangle,
  BookOpen,
  ChevronDown,
  ChevronRight,
  Download,
  FileBarChart,
  FileSpreadsheet,
  FileText,
  Landmark,
  Receipt,
  Scale,
  TrendingUp,
  type LucideIcon,
} from 'lucide-react';
import { useToast } from '../../components/ui/Toast';
import { useAuth } from '../../lib/auth';
import { buildAccountingReport, generateCSV, generateFEC, generateSIE } from '../../lib/accounting';
import { generateLiasse } from '../../lib/liasse';
import { downloadFile } from '../../lib/download';
import { fmtDate, fmtCDF } from '../../lib/format';
import { useCategories, useDeclarations, useInvoices, useTransactions } from '../../lib/hooks';
import { computeRdcComplianceOverview } from '../../lib/rdc';

type Tab = 'pnl' | 'balance' | 'journal' | 'grandlivre' | 'balancecompte' | 'lettrage' | 'syscohada';
type FoldKey = 'controls' | 'alerts' | 'classes' | 'dues' | 'grandlivre_main' | 'grandlivre_accounts' | 'balance_aux_clients' | 'balance_aux_suppliers' | 'balance_accounts' | 'lettrage_clients' | 'lettrage_suppliers';

function getBalanceTypeLabel(kind: string) {
  if (kind === 'income') return { label: 'Produit', tone: 'bg-success-50 text-success-700' };
  if (kind === 'expense') return { label: 'Charge', tone: 'bg-danger-50 text-danger-700' };
  if (kind === 'asset') return { label: 'Actif', tone: 'bg-brand-50 text-brand-700' };
  if (kind === 'liability') return { label: 'Passif', tone: 'bg-warning-50 text-warning-700' };
  if (kind === 'treasury') return { label: 'Tresorerie', tone: 'bg-accent-50 text-accent-700' };
  if (kind === 'vat') return { label: 'TVA', tone: 'bg-ink-100 text-ink-700' };
  return { label: kind, tone: 'bg-ink-100 text-ink-700' };
}

function getOpenItemTone(status: 'recent' | 'attention' | 'overdue') {
  if (status === 'overdue') return 'bg-danger-50 text-danger-700';
  if (status === 'attention') return 'bg-warning-50 text-warning-700';
  return 'bg-success-50 text-success-700';
}

function getOpenItemLabel(status: 'recent' | 'attention' | 'overdue') {
  if (status === 'overdue') return 'En retard';
  if (status === 'attention') return 'A suivre';
  return 'Recent';
}

function getMatchingTone(status: 'open' | 'partial' | 'closed') {
  if (status === 'closed') return 'bg-success-50 text-success-700';
  if (status === 'partial') return 'bg-warning-50 text-warning-700';
  return 'bg-danger-50 text-danger-700';
}

function getMatchingLabel(status: 'open' | 'partial' | 'closed') {
  if (status === 'closed') return 'Solde';
  if (status === 'partial') return 'Partiel';
  return 'Ouvert';
}

function cutoffPriorityLabel(priority: 'high' | 'medium') {
  return priority === 'high' ? 'Priorite haute' : 'Priorite moyenne';
}

function cycleStatusLabel(status: 'ok' | 'warning') {
  return status === 'ok' ? 'Cycle maitrise' : 'Cycle a revoir';
}

export function ReportsPage() {
  const { profile } = useAuth();
  const { items: transactions, loading } = useTransactions();
  const { items: categories } = useCategories();
  const { items: invoices } = useInvoices();
  const { items: declarations } = useDeclarations();
  const toast = useToast();
  const [tab, setTab] = useState<Tab>('pnl');
  const [folds, setFolds] = useState<Record<FoldKey, boolean>>({
    controls: false,
    alerts: true,
    classes: true,
    dues: true,
    grandlivre_main: true,
    grandlivre_accounts: true,
    balance_aux_clients: true,
    balance_aux_suppliers: true,
    balance_accounts: true,
    lettrage_clients: true,
    lettrage_suppliers: true,
  });

  const report = useMemo(() => buildAccountingReport(transactions, categories), [transactions, categories]);
  const ohadaForm = useMemo(() => generateLiasse('liasse_2035', transactions, categories, profile), [transactions, categories, profile]);
  const cpccForm = useMemo(() => generateLiasse('liasse_2033', transactions, categories, profile), [transactions, categories, profile]);
  const ibpForm = useMemo(() => generateLiasse('liasse_2065', transactions, categories, profile), [transactions, categories, profile]);
  const complianceOverview = useMemo(() => computeRdcComplianceOverview(profile, invoices, declarations, transactions), [profile, invoices, declarations, transactions]);
  const clientOpenItems = report.openItems.filter((item) => item.side === 'client');
  const supplierOpenItems = report.openItems.filter((item) => item.side === 'supplier');
  const payrollDueDetails = report.dueDetails.filter((item) => item.bucket === 'payroll');
  const socialDueDetails = report.dueDetails.filter((item) => item.bucket === 'social');
  const taxDueDetails = report.dueDetails.filter((item) => item.bucket === 'tax' || item.bucket === 'vat_payable' || item.bucket === 'vat_credit');
  const groupedLiabilityBreakdown = useMemo(
    () => ['42', '43', '44'].map((family) => ({ family, label: family === '42' ? 'Personnel' : family === '43' ? 'Organismes sociaux' : 'Etat et fiscalite', items: report.liabilityBreakdown.filter((item) => item.family === family) })),
    [report.liabilityBreakdown],
  );
  const ledgerAccounts = useMemo(
    () => report.balances.map((balance) => ({ ...balance, lines: report.entries.filter((entry) => entry.accountNumber === balance.accountNumber) })).filter((item) => item.lines.length > 0).sort((a, b) => b.lines.length - a.lines.length || a.accountNumber.localeCompare(b.accountNumber)),
    [report],
  );
  const auxiliarySummary = useMemo(
    () => [
      { side: 'client' as const, items: clientOpenItems, grossAmount: clientOpenItems.reduce((sum, item) => sum + item.grossAmount, 0), settledAmount: clientOpenItems.reduce((sum, item) => sum + item.settledAmount, 0), residualAmount: clientOpenItems.reduce((sum, item) => sum + item.residualAmount, 0) },
      { side: 'supplier' as const, items: supplierOpenItems, grossAmount: supplierOpenItems.reduce((sum, item) => sum + item.grossAmount, 0), settledAmount: supplierOpenItems.reduce((sum, item) => sum + item.settledAmount, 0), residualAmount: supplierOpenItems.reduce((sum, item) => sum + item.residualAmount, 0) },
    ],
    [clientOpenItems, supplierOpenItems],
  );

  const toggleFold = (key: FoldKey) => setFolds((current) => ({ ...current, [key]: !current[key] }));
  const exportJournal = () => { const content = generateFEC(transactions, categories, { siren: profile?.siren ?? null, company_name: profile?.company_name ?? null }); downloadFile(content, `journal_syscohada_${new Date().getFullYear()}.txt`, 'text/plain'); toast({ kind: 'success', message: 'Journal comptable exporte.' }); };
  const exportCpcc = () => { const content = generateSIE(report, { siren: profile?.siren ?? null, company_name: profile?.company_name ?? null }); downloadFile(content, `cpcc_${new Date().getFullYear()}.txt`, 'text/plain'); toast({ kind: 'success', message: 'Synthese CPCC exportee.' }); };
  const exportCsv = () => { const content = generateCSV(report); downloadFile(content, `grand_livre_${new Date().toISOString().slice(0, 10)}.csv`, 'text/csv'); toast({ kind: 'success', message: 'Grand livre exporte en CSV.' }); };
  const exportPdf = () => { window.print(); toast({ kind: 'info', message: 'Utilisez Imprimer puis Enregistrer en PDF.' }); };

  const tabs: { id: Tab; label: string; icon: LucideIcon }[] = [
    { id: 'pnl', label: 'Performance', icon: TrendingUp },
    { id: 'balance', label: 'Situation patrimoniale', icon: Scale },
    { id: 'journal', label: 'Journal', icon: FileText },
    { id: 'grandlivre', label: 'Grand livre', icon: BookOpen },
    { id: 'balancecompte', label: 'Balance', icon: FileBarChart },
    { id: 'lettrage', label: 'Lettrage', icon: Receipt },
    { id: 'syscohada', label: 'SYSCOHADA', icon: Landmark },
  ];

  return (
    <div className="space-y-5">
      <div className="flex flex-wrap items-end justify-between gap-3">
        <div>
          <h1 className="font-display text-2xl font-extrabold tracking-tight text-ink-950">Rapports comptables</h1>
          <p className="mt-1 text-sm text-ink-500">Journal, balance, grand livre et syntheses pour une tenue comptable orientee SYSCOHADA / CPCC.</p>
        </div>
        <div className="flex flex-wrap gap-2">
          <button onClick={exportJournal} className="btn-secondary"><FileText size={16} /> Journal</button>
          <button onClick={exportCpcc} className="btn-secondary"><FileText size={16} /> CPCC</button>
          <button onClick={exportCsv} className="btn-secondary"><FileSpreadsheet size={16} /> Balance CSV</button>
          <button onClick={exportPdf} className="btn-primary"><Download size={16} /> PDF</button>
        </div>
      </div>

      <div className="grid gap-4 md:grid-cols-5">
        <div className="card p-5"><p className="text-xs text-ink-500">Debit total</p><p className="mt-2 font-display text-2xl font-extrabold text-ink-950">{fmtCDF(report.totals.totalDebit)}</p></div>
        <div className="card p-5"><p className="text-xs text-ink-500">Credit total</p><p className="mt-2 font-display text-2xl font-extrabold text-ink-950">{fmtCDF(report.totals.totalCredit)}</p></div>
        <div className="card p-5"><p className="text-xs text-ink-500">Tresorerie</p><p className="mt-2 font-display text-2xl font-extrabold text-brand-700">{fmtCDF(report.controls.treasury)}</p></div>
        <div className="card p-5"><p className="text-xs text-ink-500">TVA nette</p><p className="mt-2 font-display text-2xl font-extrabold text-accent-700">{fmtCDF(report.controls.vatPayable > 0 ? report.controls.vatPayable : -report.controls.vatCredit)}</p></div>
        <div className="card p-5"><p className="text-xs text-ink-500">Resultat net</p><p className={`mt-2 font-display text-2xl font-extrabold ${report.pnl.netResult >= 0 ? 'text-success-700' : 'text-danger-700'}`}>{fmtCDF(report.pnl.netResult)}</p></div>
      </div>
      <div className="grid gap-4 lg:grid-cols-2 xl:grid-cols-4">
        <CollapsibleCard title="Controles comptables" open={!folds.controls} onToggle={() => toggleFold('controls')} summary={`Tresorerie ${fmtCDF(report.controls.treasury)} | Ecart ${fmtCDF(report.controls.imbalance)}`} icon={<Landmark size={18} className="text-brand-600" />}>
          <div className="grid gap-3 sm:grid-cols-2">
            <MetricCard label="Creances clients" value={fmtCDF(report.controls.receivables)} />
            <MetricCard label="Dettes fournisseurs" value={fmtCDF(report.controls.payables)} />
            <MetricCard label="Dettes personnel" value={fmtCDF(report.controls.payrollDebt)} />
            <MetricCard label="TVA collectee" value={fmtCDF(report.controls.vatCollected)} />
            <MetricCard label="TVA deductible" value={fmtCDF(report.controls.vatDeductible)} />
            <MetricCard label="Dettes sociales" value={fmtCDF(report.controls.socialDebt)} />
            <MetricCard label="Dettes fiscales" value={fmtCDF(report.controls.taxDebt)} />
            <MetricCard label="Ecart debit-credit" value={fmtCDF(report.controls.imbalance)} />
          </div>
        </CollapsibleCard>

        <CollapsibleCard title="Points de vigilance" open={!folds.alerts} onToggle={() => toggleFold('alerts')} summary={`${report.alerts.length} alerte(s)`} icon={<AlertTriangle size={18} className="text-warning-600" />}>
          <div className="space-y-3">
            {report.alerts.length === 0 ? <p className="rounded-xl bg-success-50 p-3 text-sm text-success-700">Aucune alerte bloquante sur la structure comptable actuelle.</p> : report.alerts.map((alert) => <div key={alert} className="rounded-xl bg-warning-50 p-3 text-sm text-warning-800">{alert}</div>)}
            <div className="rounded-xl bg-ink-50 p-3 text-xs text-ink-500">Operations non categorisees : {report.controls.uncategorizedTransactions} | Ecart debit-credit : {fmtCDF(report.controls.imbalance)}</div>
          </div>
        </CollapsibleCard>

        <CollapsibleCard title="Classes SYSCOHADA" open={!folds.classes} onToggle={() => toggleFold('classes')} summary={`${report.classSummaries.length} classe(s)`} icon={<BookOpen size={18} className="text-accent-700" />}>
          <div className="space-y-3">{report.classSummaries.map((item) => <div key={item.classCode} className="rounded-xl bg-ink-50 p-3"><div className="flex items-center justify-between gap-3"><div><p className="text-sm font-semibold text-ink-900">Classe {item.classCode}</p><p className="text-xs text-ink-500">{item.label}</p></div><span className="text-xs text-ink-500">{item.accountsCount} compte(s)</span></div><div className="mt-3 grid gap-2 sm:grid-cols-3 text-sm"><MetricInline label="Debit" value={fmtCDF(item.debit)} /><MetricInline label="Credit" value={fmtCDF(item.credit)} /><MetricInline label="Solde" value={fmtCDF(item.solde)} valueClassName={item.solde >= 0 ? 'text-success-700' : 'text-danger-700'} /></div></div>)}</div>
        </CollapsibleCard>

        <CollapsibleCard title="Dettes et credits a suivre" open={!folds.dues} onToggle={() => toggleFold('dues')} summary={`${report.dueDetails.length} ligne(s)`} icon={<Receipt size={18} className="text-brand-700" />}>
          <div className="space-y-4">{[{ title: 'Personnel', items: payrollDueDetails }, { title: 'Social', items: socialDueDetails }, { title: 'Fiscal / TVA', items: taxDueDetails }].map((group) => <div key={group.title} className="rounded-xl bg-ink-50 p-3"><div className="flex items-center justify-between gap-3"><p className="text-sm font-semibold text-ink-900">{group.title}</p><span className="text-xs text-ink-500">{group.items.length} ligne(s)</span></div>{group.items.length === 0 ? <p className="mt-2 text-sm text-success-700">Aucun solde ouvert.</p> : <div className="mt-2 space-y-2">{group.items.map((item) => <div key={`${group.title}-${item.accountNumber}`} className="flex items-center justify-between gap-3 text-sm"><div><p className="font-medium text-ink-900">{item.accountNumber}</p><p className="text-xs text-ink-500">{item.accountName}</p></div><p className="font-semibold text-ink-900">{fmtCDF(item.amount)}</p></div>)}</div>}</div>)}</div>
        </CollapsibleCard>
      </div>

      <div className="card p-1.5"><div className="flex gap-1 overflow-x-auto scrollbar-thin">{tabs.map((tabItem) => <button key={tabItem.id} onClick={() => setTab(tabItem.id)} className={`flex items-center gap-2 rounded-xl px-4 py-2.5 text-sm font-medium whitespace-nowrap transition ${tab === tabItem.id ? 'bg-brand-600 text-white shadow-soft' : 'text-ink-600 hover:bg-ink-100'}`}><tabItem.icon size={16} /> {tabItem.label}</button>)}</div></div>
      {loading && <div className="card h-64 skeleton" />}

      {!loading && tab === 'pnl' && <div className="grid gap-6 lg:grid-cols-2"><div className="card p-6"><h2 className="font-display text-lg font-bold text-ink-900">Produits</h2><div className="mt-4 space-y-2">{report.pnl.income.length === 0 && <p className="text-sm text-ink-500">Aucun produit enregistre.</p>}{report.pnl.income.map((item) => <div key={item.label} className="flex items-center justify-between border-b border-ink-100 py-2 text-sm"><span className="text-ink-700">{item.label}</span><span className="font-semibold text-success-700">{fmtCDF(item.amount)}</span></div>)}<div className="flex items-center justify-between border-t-2 border-ink-200 pt-3 font-display font-bold text-ink-950"><span>Total produits</span><span>{fmtCDF(report.pnl.totalIncome)}</span></div></div></div><div className="card p-6"><h2 className="font-display text-lg font-bold text-ink-900">Charges</h2><div className="mt-4 space-y-2">{report.pnl.expenses.length === 0 && <p className="text-sm text-ink-500">Aucune charge enregistree.</p>}{report.pnl.expenses.map((item) => <div key={item.label} className="flex items-center justify-between border-b border-ink-100 py-2 text-sm"><span className="text-ink-700">{item.label}</span><span className="font-semibold text-danger-700">{fmtCDF(item.amount)}</span></div>)}<div className="flex items-center justify-between border-t-2 border-ink-200 pt-3 font-display font-bold text-ink-950"><span>Total charges</span><span>{fmtCDF(report.pnl.totalExpenses)}</span></div></div></div><div className="card p-6 lg:col-span-2"><div className="flex items-center justify-between"><h2 className="font-display text-lg font-bold text-ink-900">Lecture du resultat</h2><span className={`font-display text-2xl font-extrabold ${report.pnl.netResult >= 0 ? 'text-success-700' : 'text-danger-700'}`}>{fmtCDF(report.pnl.netResult)}</span></div><div className="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-4"><MetricCard label="Produits d'exploitation" value={fmtCDF(report.pnl.operatingIncome)} /><MetricCard label="Charges d'exploitation" value={fmtCDF(report.pnl.operatingExpenses)} /><MetricCard label="Charges fiscales et sociales" value={fmtCDF(report.pnl.taxAndSocialExpenses)} /><MetricCard label="Charges du personnel" value={fmtCDF(report.pnl.personnelExpenses)} /></div></div></div>}

      {!loading && tab === 'balance' && <div className="grid gap-6 lg:grid-cols-2"><div className="card p-6"><h2 className="font-display text-lg font-bold text-ink-900">Actif</h2><div className="mt-4 space-y-2">{report.balanceSheet.assets.map((item) => <div key={item.label} className="flex items-center justify-between border-b border-ink-100 py-2 text-sm"><span className="text-ink-700">{item.label}</span><span className="font-semibold text-ink-900">{fmtCDF(item.amount)}</span></div>)}<div className="flex items-center justify-between border-t-2 border-ink-200 pt-3 font-display font-bold text-ink-950"><span>Total actif</span><span>{fmtCDF(report.balanceSheet.totalAssets)}</span></div></div></div><div className="card p-6"><h2 className="font-display text-lg font-bold text-ink-900">Passif</h2><div className="mt-4 space-y-2">{report.balanceSheet.liabilities.map((item) => <div key={item.label} className="flex items-center justify-between border-b border-ink-100 py-2 text-sm"><span className="text-ink-700">{item.label}</span><span className="font-semibold text-ink-900">{fmtCDF(item.amount)}</span></div>)}<div className="flex items-center justify-between border-b border-ink-200 py-2 text-sm"><span className="text-ink-700">Capitaux propres / resultat</span><span className="font-semibold text-ink-900">{fmtCDF(report.balanceSheet.equity)}</span></div><div className="flex items-center justify-between border-t-2 border-ink-200 pt-3 font-display font-bold text-ink-950"><span>Total passif</span><span>{fmtCDF(report.balanceSheet.totalLiabilities + report.balanceSheet.equity)}</span></div></div></div></div>}

      {!loading && tab === 'journal' && <div className="space-y-5"><div className="grid gap-4 md:grid-cols-3">{report.journals.map((journal) => <div key={journal.code} className="card p-5"><div className="flex items-center justify-between"><p className="font-display text-lg font-bold text-ink-900">{journal.code}</p><p className="text-xs text-ink-500">{journal.lines} ligne(s)</p></div><p className="mt-1 text-sm text-ink-500">{journal.label}</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><MetricCard label="Debit" value={fmtCDF(journal.debit)} /><MetricCard label="Credit" value={fmtCDF(journal.credit)} /></div></div>)}</div><div className="card overflow-hidden"><div className="border-b border-ink-100 px-5 py-4"><h2 className="font-display text-base font-bold text-ink-900">Lignes du journal</h2><p className="text-xs text-ink-500">Vue chronologique des ecritures par journal</p></div><div className="max-h-[600px] overflow-auto scrollbar-thin"><table className="w-full text-sm"><thead className="sticky top-0 bg-ink-50 text-left text-xs uppercase tracking-wider text-ink-500"><tr><th className="px-4 py-3 font-semibold">Date</th><th className="px-4 py-3 font-semibold">Journal</th><th className="px-4 py-3 font-semibold">Compte</th><th className="px-4 py-3 font-semibold">Libelle</th><th className="px-4 py-3 text-right font-semibold">Debit</th><th className="px-4 py-3 text-right font-semibold">Credit</th></tr></thead><tbody className="divide-y divide-ink-100">{report.entries.length === 0 && <tr><td colSpan={6} className="px-4 py-10 text-center text-ink-500">Aucune ecriture.</td></tr>}{report.entries.map((entry, index) => <tr key={index} className="hover:bg-ink-50/60"><td className="px-4 py-2.5 whitespace-nowrap text-ink-600">{fmtDate(entry.date)}</td><td className="px-4 py-2.5 text-ink-600">{entry.journalCode} - {entry.journalLabel}</td><td className="px-4 py-2.5 text-ink-600">{entry.accountNumber}</td><td className="px-4 py-2.5 text-ink-900">{entry.label}</td><td className="px-4 py-2.5 text-right text-danger-700">{entry.debit > 0 ? fmtCDF(entry.debit) : '-'}</td><td className="px-4 py-2.5 text-right text-success-700">{entry.credit > 0 ? fmtCDF(entry.credit) : '-'}</td></tr>)}</tbody></table></div></div></div>}
      {!loading && tab === 'grandlivre' && <div className="space-y-5"><div className="grid gap-4 xl:grid-cols-[1.2fr,0.8fr]"><CollapsibleCard title="Grand livre" open={folds.grandlivre_main} onToggle={() => toggleFold('grandlivre_main')} summary={`${report.entries.length} ecritures`} icon={<BookOpen size={18} className="text-brand-600" />}><div className="max-h-[600px] overflow-auto scrollbar-thin"><table className="w-full text-sm"><thead className="sticky top-0 bg-ink-50 text-left text-xs uppercase tracking-wider text-ink-500"><tr><th className="px-4 py-3 font-semibold">Date</th><th className="px-4 py-3 font-semibold">Journal</th><th className="px-4 py-3 font-semibold">Compte</th><th className="px-4 py-3 font-semibold">Libelle</th><th className="px-4 py-3 font-semibold text-right">Debit</th><th className="px-4 py-3 font-semibold text-right">Credit</th></tr></thead><tbody className="divide-y divide-ink-100">{report.entries.length === 0 && <tr><td colSpan={6} className="px-4 py-10 text-center text-ink-500">Aucune ecriture.</td></tr>}{report.entries.map((entry, index) => <tr key={index} className="hover:bg-ink-50/60"><td className="px-4 py-2.5 whitespace-nowrap text-ink-600">{fmtDate(entry.date)}</td><td className="px-4 py-2.5 text-ink-600">{entry.journalCode}</td><td className="px-4 py-2.5 text-ink-600">{entry.accountNumber}</td><td className="px-4 py-2.5 text-ink-900">{entry.label}</td><td className="px-4 py-2.5 text-right text-danger-700">{entry.debit > 0 ? fmtCDF(entry.debit) : '-'}</td><td className="px-4 py-2.5 text-right text-success-700">{entry.credit > 0 ? fmtCDF(entry.credit) : '-'}</td></tr>)}</tbody></table></div></CollapsibleCard><CollapsibleCard title="Grand livre par compte" open={folds.grandlivre_accounts} onToggle={() => toggleFold('grandlivre_accounts')} summary={`${ledgerAccounts.length} compte(s) mouvemente(s)`} icon={<FileBarChart size={18} className="text-accent-700" />}><div className="space-y-3">{ledgerAccounts.slice(0, 8).map((account) => <div key={account.accountNumber} className="rounded-xl bg-ink-50 p-4"><div className="flex items-center justify-between gap-3"><div><p className="font-semibold text-ink-900">{account.accountNumber} - {account.accountName}</p><p className="text-xs text-ink-500">{account.lines.length} ligne(s)</p></div><p className={`font-semibold ${account.solde >= 0 ? 'text-success-700' : 'text-danger-700'}`}>{fmtCDF(account.solde)}</p></div><div className="mt-3 space-y-2">{account.lines.slice(-3).map((line, index) => <div key={`${account.accountNumber}-${index}`} className="flex items-center justify-between gap-3 text-xs"><div><p className="font-medium text-ink-800">{fmtDate(line.date)} - {line.journalCode}</p><p className="text-ink-500">{line.label}</p></div><div className="text-right"><p className="text-ink-800">{line.debit > 0 ? fmtCDF(line.debit) : fmtCDF(line.credit)}</p><p className="text-ink-500">{line.debit > 0 ? 'Debit' : 'Credit'}</p></div></div>)}</div></div>)}{ledgerAccounts.length === 0 && <div className="rounded-xl bg-ink-50 p-4 text-sm text-ink-500">Aucun compte mouvemente.</div>}</div></CollapsibleCard></div></div>}

      {!loading && tab === 'balancecompte' && <div className="space-y-5"><div className="grid gap-4 xl:grid-cols-2"><CollapsibleCard title="Balance auxiliaire clients" open={folds.balance_aux_clients} onToggle={() => toggleFold('balance_aux_clients')} summary={`${clientOpenItems.length} ligne(s)`} icon={<Receipt size={18} className="text-brand-600" />}><div className="grid gap-3 sm:grid-cols-3"><MetricCard label="Brut" value={fmtCDF(auxiliarySummary[0].grossAmount)} /><MetricCard label="Regle" value={fmtCDF(auxiliarySummary[0].settledAmount)} /><MetricCard label="Residuel" value={fmtCDF(auxiliarySummary[0].residualAmount)} /></div><div className="mt-4 space-y-2">{auxiliarySummary[0].items.slice(0, 5).map((item) => <div key={item.id} className="rounded-xl bg-ink-50 p-3"><div className="flex items-center justify-between gap-3"><div><p className="text-sm font-semibold text-ink-900">{item.reference}</p><p className="text-xs text-ink-500">{item.count} piece(s) | {item.ageDays} j</p></div><p className="font-semibold text-ink-900">{fmtCDF(item.residualAmount)}</p></div></div>)}{auxiliarySummary[0].items.length === 0 && <div className="rounded-xl bg-success-50 p-3 text-sm text-success-800">Aucune ligne auxiliaire ouverte.</div>}</div></CollapsibleCard><CollapsibleCard title="Balance auxiliaire fournisseurs" open={folds.balance_aux_suppliers} onToggle={() => toggleFold('balance_aux_suppliers')} summary={`${supplierOpenItems.length} ligne(s)`} icon={<Receipt size={18} className="text-warning-600" />}><div className="grid gap-3 sm:grid-cols-3"><MetricCard label="Brut" value={fmtCDF(auxiliarySummary[1].grossAmount)} /><MetricCard label="Regle" value={fmtCDF(auxiliarySummary[1].settledAmount)} /><MetricCard label="Residuel" value={fmtCDF(auxiliarySummary[1].residualAmount)} /></div><div className="mt-4 space-y-2">{auxiliarySummary[1].items.slice(0, 5).map((item) => <div key={item.id} className="rounded-xl bg-ink-50 p-3"><div className="flex items-center justify-between gap-3"><div><p className="text-sm font-semibold text-ink-900">{item.reference}</p><p className="text-xs text-ink-500">{item.count} piece(s) | {item.ageDays} j</p></div><p className="font-semibold text-ink-900">{fmtCDF(item.residualAmount)}</p></div></div>)}{auxiliarySummary[1].items.length === 0 && <div className="rounded-xl bg-success-50 p-3 text-sm text-success-800">Aucune ligne auxiliaire ouverte.</div>}</div></CollapsibleCard></div><CollapsibleCard title="Balance des comptes" open={folds.balance_accounts} onToggle={() => toggleFold('balance_accounts')} summary={`${report.balances.length} compte(s)`} icon={<FileBarChart size={18} className="text-accent-700" />}><div className="overflow-x-auto scrollbar-thin"><table className="w-full text-sm"><thead className="bg-ink-50 text-left text-xs uppercase tracking-wider text-ink-500"><tr><th className="px-4 py-3 font-semibold">Compte</th><th className="px-4 py-3 font-semibold">Intitule</th><th className="px-4 py-3 font-semibold">Type</th><th className="px-4 py-3 font-semibold text-right">Debit</th><th className="px-4 py-3 font-semibold text-right">Credit</th><th className="px-4 py-3 font-semibold text-right">Solde</th><th className="px-4 py-3 font-semibold text-right">Ecritures</th></tr></thead><tbody className="divide-y divide-ink-100">{report.balances.length === 0 && <tr><td colSpan={7} className="px-4 py-10 text-center text-ink-500">Aucun compte.</td></tr>}{report.balances.map((balance) => { const typeMeta = getBalanceTypeLabel(balance.categoryKind); return <tr key={balance.accountNumber} className="hover:bg-ink-50/60"><td className="px-4 py-2.5 font-medium text-ink-900">{balance.accountNumber}</td><td className="px-4 py-2.5 text-ink-700">{balance.accountName}</td><td className="px-4 py-2.5"><span className={`chip ${typeMeta.tone}`}>{typeMeta.label}</span></td><td className="px-4 py-2.5 text-right text-ink-700">{balance.debit > 0 ? fmtCDF(balance.debit) : '-'}</td><td className="px-4 py-2.5 text-right text-ink-700">{balance.credit > 0 ? fmtCDF(balance.credit) : '-'}</td><td className={`px-4 py-2.5 text-right font-semibold ${balance.solde >= 0 ? 'text-success-700' : 'text-danger-700'}`}>{fmtCDF(balance.solde)}</td><td className="px-4 py-2.5 text-right text-ink-500">{balance.count}</td></tr>; })}</tbody></table></div></CollapsibleCard></div>}

      {!loading && tab === 'lettrage' && <div className="grid gap-4 xl:grid-cols-2"><CollapsibleCard title="Lettrage clients" open={folds.lettrage_clients} onToggle={() => toggleFold('lettrage_clients')} summary={`${clientOpenItems.length} ligne(s)`} icon={<Receipt size={18} className="text-brand-600" />}><div className="space-y-3">{clientOpenItems.length === 0 ? <div className="rounded-xl bg-success-50 p-3 text-sm text-success-800">Aucune creance client ouverte.</div> : clientOpenItems.slice(0, 12).map((item) => <div key={item.id} className="rounded-xl bg-ink-50 p-3"><div className="flex items-center justify-between gap-3"><div><p className="text-sm font-semibold text-ink-900">{item.reference}</p><p className="text-xs text-ink-500">{item.accountNumber} - {item.accountName}</p></div><div className="flex items-center gap-2"><span className={`chip ${getMatchingTone(item.matchingStatus)}`}>{getMatchingLabel(item.matchingStatus)}</span><span className={`chip ${getOpenItemTone(item.status)}`}>{getOpenItemLabel(item.status)}</span></div></div><div className="mt-3 grid gap-2 sm:grid-cols-5 text-xs text-ink-500"><MetricInline label="Total" value={fmtCDF(item.grossAmount)} /><MetricInline label="Regle" value={fmtCDF(item.settledAmount)} /><MetricInline label="Residuel" value={fmtCDF(item.residualAmount)} /><MetricInline label="Pieces" value={String(item.count)} /><MetricInline label="Anciennete" value={`${item.ageDays} j`} /></div><p className="mt-2 text-xs text-ink-500">Plus ancienne: {fmtDate(item.oldestDate)} | Plus recente: {fmtDate(item.newestDate)}</p></div>)}</div></CollapsibleCard><CollapsibleCard title="Lettrage fournisseurs" open={folds.lettrage_suppliers} onToggle={() => toggleFold('lettrage_suppliers')} summary={`${supplierOpenItems.length} ligne(s)`} icon={<Receipt size={18} className="text-warning-600" />}><div className="space-y-3">{supplierOpenItems.length === 0 ? <div className="rounded-xl bg-success-50 p-3 text-sm text-success-800">Aucune dette fournisseur ouverte.</div> : supplierOpenItems.slice(0, 12).map((item) => <div key={item.id} className="rounded-xl bg-ink-50 p-3"><div className="flex items-center justify-between gap-3"><div><p className="text-sm font-semibold text-ink-900">{item.reference}</p><p className="text-xs text-ink-500">{item.accountNumber} - {item.accountName}</p></div><div className="flex items-center gap-2"><span className={`chip ${getMatchingTone(item.matchingStatus)}`}>{getMatchingLabel(item.matchingStatus)}</span><span className={`chip ${getOpenItemTone(item.status)}`}>{getOpenItemLabel(item.status)}</span></div></div><div className="mt-3 grid gap-2 sm:grid-cols-5 text-xs text-ink-500"><MetricInline label="Total" value={fmtCDF(item.grossAmount)} /><MetricInline label="Regle" value={fmtCDF(item.settledAmount)} /><MetricInline label="Residuel" value={fmtCDF(item.residualAmount)} /><MetricInline label="Pieces" value={String(item.count)} /><MetricInline label="Anciennete" value={`${item.ageDays} j`} /></div><p className="mt-2 text-xs text-ink-500">Plus ancienne: {fmtDate(item.oldestDate)} | Plus recente: {fmtDate(item.newestDate)}</p></div>)}</div></CollapsibleCard></div>}
      {!loading && tab === 'syscohada' && (
        <div className="grid gap-6 xl:grid-cols-2">
          <div className="card p-6 xl:col-span-2"><div className="flex items-start justify-between gap-3"><div><h2 className="font-display text-lg font-bold text-ink-900">Conformite facture normalisee RDC</h2><p className="mt-1 text-xs text-ink-500">Lecture du socle attendu pour la fiscalite congolaise, la facture normalisee et la piste d audit.</p></div><span className={`chip ${complianceOverview.score >= 80 ? 'bg-success-50 text-success-700' : complianceOverview.score >= 60 ? 'bg-warning-50 text-warning-700' : 'bg-danger-50 text-danger-700'}`}>{complianceOverview.score}%</span></div><div className="mt-4 grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Factures normalisees" value={String(complianceOverview.normalizedInvoices)} /><MetricCard label="Declarations a traiter" value={String(complianceOverview.pendingDeclarations)} /><MetricCard label="Operations non classees" value={String(complianceOverview.uncategorizedTransactions)} /><MetricCard label="Champs profil manquants" value={String(complianceOverview.missingProfileFields.length)} /></div><div className="mt-4 grid gap-4 lg:grid-cols-2"><div className="rounded-xl bg-ink-50 p-4"><p className="text-sm font-semibold text-ink-900">Profil fiscal et dispositif</p><div className="mt-3 space-y-2">{complianceOverview.missingProfileFields.length === 0 ? <p className="text-sm text-success-700">Profil fiscal principal complet.</p> : complianceOverview.missingProfileFields.map((field) => <div key={field.key} className="rounded-xl bg-warning-50 px-3 py-2 text-sm text-warning-800">{field.label}</div>)}</div></div><div className="rounded-xl bg-ink-50 p-4"><p className="text-sm font-semibold text-ink-900">Derniere facture analysee</p>{complianceOverview.latestInvoice ? <div className="mt-3 space-y-2 text-sm text-ink-700"><p><span className="font-semibold text-ink-900">Numero:</span> {complianceOverview.latestInvoice.number}</p><p><span className="font-semibold text-ink-900">Date:</span> {fmtDate(complianceOverview.latestInvoice.issue_date)}</p><p><span className="font-semibold text-ink-900">Client:</span> {complianceOverview.latestInvoice.customer_name}</p><p><span className="font-semibold text-ink-900">Conformite:</span> {complianceOverview.invoiceCompliance.score}%</p></div> : <p className="mt-3 text-sm text-ink-500">Aucune facture disponible pour l analyse.</p>}</div></div><div className="mt-4 rounded-2xl bg-ink-50 p-4"><div className="flex items-center justify-between gap-3"><p className="text-sm font-semibold text-ink-900">Checks facture / DGI</p><span className="text-xs text-ink-500">{complianceOverview.invoiceCompliance.checks.length} controle(s)</span></div><div className="mt-3 grid gap-2 md:grid-cols-2">{complianceOverview.invoiceCompliance.checks.length === 0 ? <p className="text-sm text-ink-500">Aucun controle disponible.</p> : complianceOverview.invoiceCompliance.checks.map((check) => <div key={check.label} className={`rounded-xl px-3 py-2 text-sm ${check.ok ? 'bg-success-50 text-success-800' : 'bg-danger-50 text-danger-800'}`}>{check.label}</div>)}</div></div><div className="mt-4 rounded-2xl bg-brand-50 p-4 ring-1 ring-brand-100"><p className="text-sm font-semibold text-brand-900">Actions recommandees</p><div className="mt-3 space-y-2">{complianceOverview.recommendations.length === 0 ? <p className="text-sm text-brand-800">Le socle minimum du cahier de charge est en bonne voie sur la partie fiscale et comptable.</p> : complianceOverview.recommendations.map((item) => <div key={item} className="rounded-xl bg-white px-3 py-2 text-sm text-ink-700">{item}</div>)}</div></div></div><div className="card p-6"><h2 className="font-display text-lg font-bold text-ink-900">Classes SYSCOHADA</h2><div className="mt-4 space-y-3">{report.classSummaries.map((item) => <div key={item.classCode} className="rounded-xl bg-ink-50 p-4"><div className="flex items-center justify-between gap-3"><div><p className="font-semibold text-ink-900">Classe {item.classCode}</p><p className="text-xs text-ink-500">{item.label}</p></div><span className="text-xs text-ink-500">{item.accountsCount} compte(s)</span></div><div className="mt-3 grid gap-3 sm:grid-cols-3 text-sm"><MetricInline label="Debit" value={fmtCDF(item.debit)} /><MetricInline label="Credit" value={fmtCDF(item.credit)} /><MetricInline label="Solde" value={fmtCDF(item.solde)} valueClassName={item.solde >= 0 ? 'text-success-700' : 'text-danger-700'} /></div></div>)}</div></div>
          <div className="card p-6"><h2 className="font-display text-lg font-bold text-ink-900">Dettes sociales et fiscales detaillees</h2><div className="mt-4 space-y-3">{report.dueDetails.length === 0 ? <div className="rounded-xl bg-success-50 p-3 text-sm text-success-800">Aucune dette ou credit detaille a suivre.</div> : report.dueDetails.map((item) => <div key={`${item.bucket}-${item.accountNumber}`} className="rounded-xl bg-ink-50 p-4"><div className="flex items-center justify-between gap-3"><div><p className="font-semibold text-ink-900">{item.accountNumber} - {item.accountName}</p><p className="text-xs text-ink-500">{item.bucketLabel}</p></div><p className="font-semibold text-ink-900">{fmtCDF(item.amount)}</p></div></div>)}</div></div>
          <div className="card p-6"><h2 className="font-display text-lg font-bold text-ink-900">Ventilation comptes 42 / 43 / 44</h2><div className="mt-4 space-y-4">{groupedLiabilityBreakdown.map((group) => <div key={group.family} className="rounded-xl bg-ink-50 p-4"><div className="flex items-center justify-between gap-3"><div><p className="font-semibold text-ink-900">Classe {group.family}</p><p className="text-xs text-ink-500">{group.label}</p></div><span className="text-xs text-ink-500">{group.items.length} compte(s)</span></div>{group.items.length === 0 ? <p className="mt-3 text-sm text-success-700">Aucun solde ouvert sur cette famille.</p> : <div className="mt-3 space-y-2">{group.items.map((item) => <div key={item.accountNumber} className="rounded-xl bg-white px-4 py-3 ring-1 ring-ink-100"><div className="flex items-center justify-between gap-3"><div><p className="text-sm font-semibold text-ink-900">{item.accountNumber} - {item.accountName}</p><p className="text-xs text-ink-500">Debit {fmtCDF(item.debit)} | Credit {fmtCDF(item.credit)}</p></div><span className={`chip ${item.status === 'credit' ? 'bg-warning-50 text-warning-700' : item.status === 'debit' ? 'bg-brand-50 text-brand-700' : 'bg-success-50 text-success-700'}`}>{item.status === 'credit' ? 'Solde crediteur' : item.status === 'debit' ? 'Solde debiteur' : 'Solde nul'}</span></div><p className={`mt-2 text-sm font-semibold ${item.balance > 0 ? 'text-warning-700' : item.balance < 0 ? 'text-brand-700' : 'text-success-700'}`}>{fmtCDF(item.balance)}</p></div>)}</div>}</div>)}</div></div>
          <div className="card p-6"><h2 className="font-display text-lg font-bold text-ink-900">Pre-cloture comptable</h2><div className="mt-4 space-y-3">{report.preClosingActions.length === 0 ? <div className="rounded-xl bg-success-50 p-3 text-sm text-success-800">Aucune action de pre-cloture prioritaire detectee.</div> : report.preClosingActions.map((action) => <div key={action.id} className="rounded-xl bg-ink-50 p-4"><div className="flex items-center justify-between gap-3"><div><p className="font-semibold text-ink-900">{action.label}</p><p className="text-xs text-ink-500">{action.timing}</p></div><span className={`chip ${action.priority === 'high' ? 'bg-danger-50 text-danger-700' : 'bg-warning-50 text-warning-700'}`}>{cutoffPriorityLabel(action.priority)}</span></div><p className="mt-2 text-sm text-ink-600">{action.detail}</p><div className="mt-3 grid gap-2 text-xs text-ink-500 sm:grid-cols-3"><MetricInline label="Montant / base" value={fmtCDF(action.amount)} /><MetricInline label="Debit propose" value={action.debitAccount} /><MetricInline label="Credit propose" value={action.creditAccount} /></div></div>)}</div></div><div className="card p-6"><h2 className="font-display text-lg font-bold text-ink-900">Ecritures d inventaire estimees</h2><div className="mt-4 space-y-3">{report.yearEndAdjustments.length === 0 ? <div className="rounded-xl bg-success-50 p-3 text-sm text-success-800">Aucune ecriture d inventaire estimee automatiquement.</div> : report.yearEndAdjustments.map((adjustment) => <div key={adjustment.id} className="rounded-xl bg-ink-50 p-4"><div className="flex items-center justify-between gap-3"><div><p className="font-semibold text-ink-900">{adjustment.label}</p><p className="text-xs text-ink-500">{adjustment.detail}</p></div><span className={`chip ${adjustment.priority === 'high' ? 'bg-danger-50 text-danger-700' : 'bg-warning-50 text-warning-700'}`}>{cutoffPriorityLabel(adjustment.priority)}</span></div><div className="mt-3 grid gap-2 text-xs text-ink-500 sm:grid-cols-4"><MetricInline label="Montant estime" value={fmtCDF(adjustment.amount)} /><MetricInline label="Base detectee" value={String(adjustment.basisCount)} /><MetricInline label="Debit propose" value={adjustment.debitAccount} /><MetricInline label="Credit propose" value={adjustment.creditAccount} /></div></div>)}</div></div><div className="card p-6"><h2 className="font-display text-lg font-bold text-ink-900">Revues par cycle de cloture</h2><div className="mt-4 space-y-3">{report.cycleReviews.map((cycle) => <div key={cycle.id} className="rounded-xl bg-ink-50 p-4"><div className="flex items-center justify-between gap-3"><div><p className="font-semibold text-ink-900">{cycle.label}</p><p className="text-xs text-ink-500">{cycle.detail}</p></div><span className={`chip ${cycle.status === 'ok' ? 'bg-success-50 text-success-700' : 'bg-warning-50 text-warning-700'}`}>{cycleStatusLabel(cycle.status)}</span></div><div className="mt-3 grid gap-2 text-xs text-ink-500 sm:grid-cols-3"><MetricInline label="Montant expose" value={fmtCDF(cycle.amount)} /><MetricInline label="Point(s) a suivre" value={String(cycle.count)} /><MetricInline label="Comptes focus" value={cycle.focusAccounts.join(', ')} /></div><div className="mt-3 grid gap-2">{cycle.checkpoints.map((checkpoint) => <div key={checkpoint} className="rounded-xl bg-white px-3 py-2 text-sm text-ink-700 ring-1 ring-ink-100">{checkpoint}</div>)}</div></div>)}</div></div><div className="card p-6"><h2 className="font-display text-lg font-bold text-ink-900">Rattachement a l exercice</h2><div className="mt-4 space-y-3">{report.cutoffSignals.length === 0 ? <div className="rounded-xl bg-success-50 p-3 text-sm text-success-800">Aucun signal majeur de rattachement detecte.</div> : report.cutoffSignals.map((signal) => <div key={signal.id} className="rounded-xl bg-ink-50 p-4"><div className="flex items-center justify-between gap-3"><div><p className="font-semibold text-ink-900">{signal.label}</p><p className="text-xs text-ink-500">{signal.detail}</p></div><span className={`chip ${signal.priority === 'high' ? 'bg-danger-50 text-danger-700' : 'bg-warning-50 text-warning-700'}`}>{cutoffPriorityLabel(signal.priority)}</span></div><div className="mt-3 grid gap-2 text-xs text-ink-500 sm:grid-cols-3"><MetricInline label="Montant estime" value={fmtCDF(signal.amount)} /><MetricInline label="Debit propose" value={signal.debitAccount} /><MetricInline label="Credit propose" value={signal.creditAccount} /></div></div>)}</div></div><div className="card p-6 xl:col-span-2"><div className="flex items-start justify-between gap-3"><div><h2 className="font-display text-lg font-bold text-ink-900">Synthese OHADA / CPCC / IBP</h2><p className="mt-1 text-xs text-ink-500">Lecture annuelle pour les etats SYSCOHADA, le CPCC et l impot sur les benefices.</p></div><span className="chip bg-accent-50 text-accent-700">Exercice 2026</span></div><div className="mt-4 grid gap-4 lg:grid-cols-3"><div className="rounded-2xl bg-ink-50 p-4"><p className="text-sm font-semibold text-ink-900">OHADA / SYSCOHADA</p><p className="mt-1 text-xs text-ink-500">{ohadaForm.title}</p><div className="mt-3 space-y-2">{ohadaForm.lines.slice(0, 5).map((line) => <div key={line.code} className="flex items-center justify-between gap-3 text-sm"><span className="text-ink-600">{line.label}</span><span className="font-semibold text-ink-900">{fmtCDF(line.amount)}</span></div>)}</div><div className="mt-3 rounded-xl bg-white px-4 py-3 ring-1 ring-ink-100"><MetricInline label="Resultat avant impot" value={fmtCDF(ohadaForm.total)} /></div></div><div className="rounded-2xl bg-ink-50 p-4"><p className="text-sm font-semibold text-ink-900">CPCC</p><p className="mt-1 text-xs text-ink-500">{cpccForm.title}</p><div className="mt-3 space-y-2">{cpccForm.lines.slice(0, 6).map((line) => <div key={line.code} className="flex items-center justify-between gap-3 text-sm"><span className="text-ink-600">{line.label}</span><span className="font-semibold text-ink-900">{fmtCDF(line.amount)}</span></div>)}</div><div className="mt-3 rounded-xl bg-white px-4 py-3 ring-1 ring-ink-100"><MetricInline label="IBP estime CPCC" value={fmtCDF(cpccForm.total)} /></div></div><div className="rounded-2xl bg-ink-50 p-4"><p className="text-sm font-semibold text-ink-900">IBP / fiscalite RDC</p><p className="mt-1 text-xs text-ink-500">{ibpForm.title}</p><div className="mt-3 space-y-2">{ibpForm.lines.map((line) => <div key={line.code} className="flex items-center justify-between gap-3 text-sm"><span className="text-ink-600">{line.label}</span><span className="font-semibold text-ink-900">{line.code === 'IBP-04' ? `${line.amount.toFixed(0)}%` : fmtCDF(line.amount)}</span></div>)}</div><div className="mt-3 rounded-xl bg-brand-50 px-4 py-3 ring-1 ring-brand-200"><MetricInline label="Impot a payer estime" value={fmtCDF(ibpForm.total)} valueClassName="text-brand-800" /></div></div></div></div>
          <div className="card p-6 xl:col-span-2"><div className="flex items-start justify-between gap-3"><div><h2 className="font-display text-lg font-bold text-ink-900">Regularisations conseillees</h2><p className="mt-1 text-xs text-ink-500">Propositions d ecritures a valider selon les pieces, declarations DGI et bordereaux sociaux.</p></div><span className="chip bg-brand-50 text-brand-700">{report.recommendedEntries.length} proposition(s)</span></div><div className="mt-4 grid gap-3 lg:grid-cols-2">{report.recommendedEntries.length === 0 ? <div className="rounded-xl bg-success-50 p-3 text-sm text-success-800 lg:col-span-2">Aucune regularisation proposee sur la base des soldes actuels.</div> : report.recommendedEntries.map((entry) => <div key={entry.id} className="rounded-xl bg-ink-50 p-4"><div className="flex items-center justify-between gap-3"><div><p className="font-semibold text-ink-900">{entry.label}</p><p className="text-xs text-ink-500">Journal {entry.journalCode}</p></div><p className="font-semibold text-ink-900">{fmtCDF(entry.amount)}</p></div><div className="mt-3 grid gap-2 text-xs text-ink-500 sm:grid-cols-2"><MetricInline label="Debit" value={entry.debitAccount} /><MetricInline label="Credit" value={entry.creditAccount} /></div><p className="mt-2 text-sm text-ink-600">{entry.rationale}</p></div>)}</div></div>
        </div>
      )}
    </div>
  );
}

function CollapsibleCard({ title, summary, open, onToggle, icon, children }: { title: string; summary: string; open: boolean; onToggle: () => void; icon: ReactNode; children: ReactNode }) {
  return <div className="card p-5"><button type="button" onClick={onToggle} className="flex w-full items-center justify-between gap-3 text-left"><div className="flex items-center gap-2">{icon}<div><h2 className="font-display text-base font-bold text-ink-900">{title}</h2><p className="text-xs text-ink-500">{summary}</p></div></div>{open ? <ChevronDown size={18} className="text-ink-500" /> : <ChevronRight size={18} className="text-ink-500" />}</button>{open && <div className="mt-4">{children}</div>}</div>;
}

function MetricCard({ label, value }: { label: string; value: string }) {
  return <div className="rounded-xl bg-ink-50 p-4"><p className="text-xs text-ink-500">{label}</p><p className="mt-1 font-display text-xl font-extrabold text-ink-900">{value}</p></div>;
}

function MetricInline({ label, value, valueClassName = 'text-ink-900' }: { label: string; value: string; valueClassName?: string }) {
  return <div><p className="text-xs text-ink-500">{label}</p><p className={`mt-1 font-semibold ${valueClassName}`}>{value}</p></div>;
}





